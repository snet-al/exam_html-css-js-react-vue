import PhotoItem from "../PhotoItem/PhotoItem";

const PhotoContainer = () => {
  return (
    <section>
      <PhotoItem />
      <PhotoItem />
      <PhotoItem />
      <PhotoItem />
    </section>
  );
};

export default PhotoContainer;
