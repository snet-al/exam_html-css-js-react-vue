import React from "react";

function LoadMorePhotos() {
  const handleLoadMorePhotos = () => {};

  return (
    <button
      className="load-more-button-container"
      onClick={handleLoadMorePhotos}
    >
      Load More Photos
    </button>
  );
}

export default LoadMorePhotos;
